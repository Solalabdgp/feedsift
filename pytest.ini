[pytest]
testpaths = tests
# Иначе каждый async-тест пришлось бы помечать вручную, и забытая пометка
# молча проходит как «тест ничего не проверил».
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    error::DeprecationWarning:app.*
addopts = -q --strict-markers
